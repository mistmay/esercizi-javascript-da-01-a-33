const person = {
  firstName: 'John',
  lastName: 'Doe'
};
/*
I have to put the solution in a comment
because Beautify is not recognizing the syntax
for the optional-chain and it does not let me save
without changing the formatation of the code

================================
console.log(person.address?.city);
console.log(person.fullName?.());
=========================================
*/